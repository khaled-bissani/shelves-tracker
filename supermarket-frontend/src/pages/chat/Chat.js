import { ScrollView} from "react-native";
import SingleContact from "../../components/SingleContact/SingleContact";

const Chat = () => {
    return <ScrollView>
        <SingleContact/>
    </ScrollView>
}

export default Chat;